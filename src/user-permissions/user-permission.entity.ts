import { Entity, PrimaryGeneratedColumn, Column, OneToMany, CreateDateColumn, UpdateDateColumn } from 'typeorm';
import { MenuOption } from '../menu-options/menu-option.entity';

@Entity('user_permissions')
export class UserPermission {
  // The rest of the UserPermission entity code should be here
  // However, it was not provided in the original code snippet
  // Please insert the complete UserPermission entity code below
}